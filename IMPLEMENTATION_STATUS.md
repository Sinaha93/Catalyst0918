# 구현·검증 현황

## 현재 상태 요약 — 2026-09-21

이 절과 [업무 규칙](BUSINESS_RULES.md)이 현재 기준입니다. 아래 날짜별 절은 당시의 작업 이력이며, 과거의 미확정·미등록·PPT 설명을 현재 상태로 해석하지 않습니다.

- 실행: `촉매마감관리_실행.lnk` → 서명이 유효한 설치된 `pythonw.exe` → 최신 `launcher.py`. 검은 콘솔 창 없이 브라우저 실행. 8765 포트에서 UI 자산, 상태, 자료 취소 영향 조회 API 및 브라우저의 화면 데이터 요청 200 응답 확인.
- 보안: 기존 EXE와 새 EXE 모두 NotSigned. 새 BOM연결 EXE는 Smart App Control에 의해 차단됨. 이전 파일 허용과 새 파일 차단의 정확한 클라우드 평가 차이는 확인 불가. 보안 설정 변경 없음. Python 개발 실행은 정상 확인했지만 새 EXE 실행 검증은 미완료.
- 시험 중 8766 포트가 다른 프로그램에 사용돼 시작 오류 발생. 해당 프로그램은 변경하지 않았고 기존 촉매 서버를 정상 종료한 뒤 8765에서 새 바로가기로 실행 확인. 이 오류는 EXE의 게시자 차단과 별개.
- 운영 DB: 등록 원본 9개, 2026-08 확정 v1 1건 존재(2026-09-21 14:31 확정). 이번 작업에서 확정본을 변경하거나 새 수정 마감을 생성하지 않음.
- 새 BOM·구매단가 파일은 원본 등록 완료, 계산 반영 전. **적용 기준일(8월 1일 또는 9월 1일 등)을 사용자에게 확인 중**. 답변 없이 날짜를 추정해 반영하지 않음.
- 실제 입력 검증: BOM 195품번 / 마감처 연결 153개 / 구매단가 195개, 누락·충돌 없음. 기존 단가 대비 28533-2CCC0 591,766원만 신규. 기존 월계획 등록에서 이 품번 0행을 제외한 결정과는 별개.
- 과거 누락 4품번은 사용자 승인 후 등록 완료: 28533-08910=1원, 28533-08920=1원, 28957-03CR0=41,617원, 28957-04DA0=104,878원(2026-08-01).
- 자료 삭제·반영 취소·복원 및 연결 자료 영향 표시 구현. 실제 업무 자료는 이번 작업에서 삭제하지 않음. 원본·확정 마감·출력 파일 보존.
- 최신 자동 테스트 46개 통과, TypeScript 검사·프론트엔드 빌드 통과. 실제 DB 복사본에서 새 마스터 반영·취소·복원 시 거래처 합계, 확정 내역, 예상 단가 유지 확인. Python 실행에서 화면 제공/API 확인. 새로운 삭제 UI의 전체 수동 조작 검증은 미완료.
- Git에는 코드·테스트·문서만 보관. 업무 엑셀·매크로 파일·PPT·DB·백업·EXE·바로가기·빌드 산출물은 제외. Git 푸시가 업무 데이터 백업을 대체하지 않음.

### 다음 작업

1. 사용자에게 새 구매단가 적용 기준일을 확인하고, 변경 미리보기 확인 후 운영 반영.
2. 자료 취소·복원의 실제 사용 흐름 확인. 현재 업무 자료를 시험 목적으로 취소하지 않음.
3. 신규 월 원본과 양식 변경, 수동 수정 충돌, 출력 길이 및 과거 누적 수치의 운영 검증 계속.
4. 다른 PC에 단일 EXE를 배포하려면 게시자 코드 서명 및 해당 PC의 실행 정책 검증 필요. 기존 바로가기는 현재 로컬 개발 환경용.

## 날짜별 구현 이력

## 현재 운영 방식: 엑셀 전용

- 사용자 요청으로 PPT 생성·등록·다운로드 UI 및 생성 엔진을 제거. 기존 PPT 원본·보관 파일·출력 파일은 보존.
- 새 확정은 Excel 양식만 보관. 기존 Excel/PPT 양식을 함께 보관한 확정 내역도 Excel만 사용하여 재출력 가능.
- Excel 그래프·납품처별 소계·예상 정산 참고표 유지. 아래 PPT 관련 설명은 이전 구현 이력이며 현재 기능이 아님.
- PPT 양식을 배포 실행 파일에서 제외. 출력에 Windows Excel만 필요.

## 미정산 보고서 납품처 소계

- Excel·PPT가 같은 납품처별 상세/소계 데이터 사용. 전월 이월이 있고 당월 전량 정산된 품목도 표시.
- Excel 소계는 상세 SUM 수식으로 작성. 출력 시 납품처별 수량·금액을 확정 스냅샷과 대사.
- 상세행 배경색·조건부서식 잔재 제거, 흰색 상세행과 연한 색 소계행으로 통일. PPT는 기존 양식 본문 글꼴·크기 유지.
- 작은 납품처 묶음은 같은 페이지에 유지, 긴 묶음은 분할하되 소계 단독 페이지 방지.
- 자동 테스트 36개 통과. 실제 8월 확정 스냅샷 복사본으로 Excel 및 PPT 소계·렌더링 검증. 운영 확정 내역 변경 없음.

## 2026-09-21 계획 등록 및 그래프 시트명 정리

- 사용자 승인으로 8월 계획 152행 등록. 계획 수량 153,698개, 금액 23,296,997,754원. 0수량·0금액인 미등록 품번 28533-2CCC0 1행 제외. 출처 파일명 20260921은 작성일로만 처리.
- 계획 등록 전 DB 백업, 등록 후 실제 단가·입고·정산·미결·예상금액 불변과 중복 방지 검증. 운영 마감은 미확정 유지.
- 원본 Excel의 `종합2`를 `월별 계획·실적`으로 변경. 기존 이름의 양식도 입력·출력 코드에서 호환.
- 하단 B66:AT101 표는 B104의 TRANSPOSE를 통해 종합 O7:O16, 기아 화성 평균값 수식에서 사용 중이므로 삭제하지 않음.
- 변경 전 원본을 backups에 보관. Excel 재계산 후 전체 셀 저장값 차이 0건, 14개 시트·차트·피벗 유지, 셀 오류 0건. 자동 테스트 33개 통과.

## 콘솔 없는 백그라운드 실행

- PyInstaller 기본 빌드를 windowed 모드로 변경. 새 배포 파일 `촉매마감관리_백그라운드.exe` 생성.
- Uvicorn 콘솔 출력 의존 제거. `logs/application.log`에 회전 로그 보관(2MB × 본파일 및 이전 3개).
- UI의 프로그램 종료로 백업 후 서버 종료. 재실행 시 기존 서버에 연결하는 동작 유지.
- 테스트 28개 통과. 새 실행 파일의 Windows GUI subsystem, 격리 DB에서 API/화면 자산 제공, 중복 실행 종료, 로그 생성, 정상 종료 확인.
- 기존 실행 파일과 운영 DB는 이번 변경으로 덮어쓰지 않음.

## 2026-09-18 실제 8월 자료 초기 이관

- `catalyst/august_bootstrap.py`는 확인된 2026-08 전용 초기 이관 모듈. 월별 범용 자동 입력 완료를 의미하지 않음.
- 원본 보관 및 백업 후 실제 634행(마스터·단가·ERP 입고·기존 정산·이월)을 운영 DB에 반영. 기존 마감에서 가져온 정산/이월은 원본 파일·시트·행과 초기 이관 표식을 보존.
- 촉매사 정산수량 대사, ERP 거래처·품번별 잔량, 계산서 단가·금액 검증 후 원자적 반영. 동일 입력 재반영은 중복 차단. 이미 업무 자료가 있는 저장소의 다른 초기 이관은 거부.
- 원본 파일명 변경 허용(명시된 역할의 시트·내용 검증). 8월 이외 또는 양식 변경은 자동 적용하지 않음.
- 운영 검증: 이월 5,518 / 입고 112,428 / 정산 105,925 / 미결 12,021 / 정산금액 14,712,756,099원.
- 2JDF0: 미결 7,487개, 등록가 기준 7,487원, 예상 정산금액 984,675,266원.
- 초기 이월 평가에는 확인된 8월 마스터 단가 사용. 기존 종합 시트의 전월 이월 **금액**을 그대로 복제하지 않았으며 기존 보고서 금액 전부와 일치한다고 주장하지 않음.
- 마스터 누락 4품번(28533-08910, 28533-08920, 28957-03CR0, 28957-04DA0)은 임의 생성하지 않아 마감 확정을 차단. 앞의 2개 품번 ERP 입고 단가도 미확정으로 표시. 총 10개 원본별 확인 항목.
- 자동 테스트 27개 통과(실제 8월 자료 검증·중복 차단·실패 원자성·파일명 변경 포함).
- 같은 새 실행 파일을 다시 정상 실행했고, 실행 중 API에서 실제 DB의 2JDF0 잔량/예상금액을 확인함. 과거 WinError 4551은 당시 시도에 한정되며 현재 지속 차단 상태가 아님.

## 2026-09-18 단가 분리

- 마스터 관리에 월·품번·거래처별 예상 정산 단가 입력/수정/비활성 기능 추가. 변경 사유와 전후 이력을 저장.
- 예상 정산금액은 당월 미결 수량 × 예상 단가로 별도 표시. 실제 거래 단가·정산금액·다음 달 이월금액은 변경하지 않음.
- 확정 마감에는 추정 단가 및 계산 결과도 동결. 추정값 변경 시 기존 미리보기로 확정 불가.
- 2JDF0: 현재 가단가 1원과 예상 단가 131,518원을 구분. 월말 잔량 7,487개 테스트에서 기록 기준 7,487원, 추정 984,675,266원 확인.
- 2JBE0 / 3LVD0의 미정산 시트 단가는 입력 오류이며 현재 마스터 62,513원 / 165,861원이 기준. 원본은 변경하지 않음.
- 테스트 24개 통과 및 React 타입 검사/빌드 성공. 실제 정산이 존재할 때도 추정 단가가 침투하지 않고, 실제 단가 누락을 추정 단가로 숨기지 않는지 확인.
- `촉매마감관리_단가분리.exe` 별도 빌드 완료. 실행 점검은 Windows 애플리케이션 제어 정책(WinError 4551)으로 차단되어 배포 실행 검증 미완료. 기존 실행 파일은 덮어쓰지 않았음.
- 차단 원인 확인: 2026-09-18 10:59:59 CodeIntegrity 이벤트 3077이 위 실행 파일을 지목. 정책 ID `0283ac0f-fff1-49ae-ada1-8a933130cad6`는 활성화된 `VerifiedAndReputableDesktop`(Smart App Control). 파일은 Authenticode 서명이 없으며 인터넷 다운로드 표식(Zone.Identifier)도 없음. 파일 손상으로 단정하지 않으며, 정식 신뢰 가능한 코드 서명과 재검증이 필요. 보안 정책/백신 설정은 변경하지 않았고 다른 실행 경로로 우회하지 않음.
- 운영 DB 자동 백업 후 2026-08 / 현대 울산 / 28957-2JDF0의 추정 단가 131,518원만 등록. 실제 거래·수량·단가 및 원본 파일은 변경하지 않음.
- 예상 금액은 화면·확정 스냅샷과 Excel 별도 참고표, PPT 별도 참고 페이지에 반영. 기존 본문 금액은 기록 기준을 유지하며 실제 정산·다음 달 이월에 추정값을 합산하지 않음.

## 2026-09-18 예상 정산금액 보고서 검증

- 자동 테스트 32개 통과, React 타입 검사·빌드 성공.
- 운영 DB 복사본으로 8월 보고서 생성. Excel 14개 시트, PPT 7장 및 내장 거래처 자료 10개 Office 열기 검사 통과.
- 2JDF0 기록 기준 7,487원 / 예상 984,675,266원 Excel·PPT 대사 및 화면 확인.
- 이관 증빙은 보존하고, 보고서 업무 비고에서 내부 이관 문구만 제거.
- 실제 운영 마감은 확정하지 않음. 시험 출력은 work 아래 격리 보관.
- 월별 계획 자료 미등록으로 당월 계획 대비 실적은 아직 검증 완료가 아님. 계획 미등록이 차트에서 0으로 보이는 기존 표시 문제도 후속 수정 필요. 전체 보고서의 최종 배포본으로 판단하지 않음.

## 실제 자료 확인으로 갱신된 업무 기준

- 주문전송은 일일 발주 확인용으로 마감 필수 입력에서 제외.
- ERP 입고 대사: 유미코아 `납품 Summary/Total 납품`, 희성 `8월 마감자료/출하`. CONVERTER 2품목 829개 제외.
- 희성의 `계`는 입고 대사가 아니라 정산수량 확인에 사용.
- 28991-4C520은 촉매사 유미코아, 실제 마감 상대 전주공장 직사급으로 분리.
- 기존 8월 마감 엑셀과 ERP의 거래처·품번별 수량 129개 조합 일치. 정산 상세 86행의 단가·금액 일치. 과거 파일의 저장값 대사이며 재계산 검증과는 별개.
- 위 업무 규칙을 모두 실제 파일 전용 입력 모듈에 연결한 것은 아님. 아래의 최초 구현 현황 중 원본 부족 설명은 이 확인 내용으로 대체.

## 제공한 구현

- FastAPI / SQLite / React·TypeScript, Windows 단일 실행 파일
- 업로드·실행 중 폴더 감시·시트와 제목 행 기반 양식 저장
- 원본 해시 보관, 동일 파일 중복 차단, 월별 자료 묶음 교체
- 날짜 열 없는 양식 및 수동 수정 충돌의 자동 반영 차단
- 직접 입력·엑셀 붙여넣기·수정 이력, 품번·거래처·공장·차종·공급업체 연결
- 십진수 계산, 적용일 단가, 전월 확정 이월, 단가 누락·음수 미결 차단
- 확정 스냅샷·수정 마감 버전·보고서 양식 해시 보존
- 기존 마감 결과 비교 및 원본 파일·시트·행 근거 조회
- 기존 엑셀 14개 시트, 본문 PPT 및 내장 거래처 자료 10개 출력
- 미정산 본문 표 18행 단위 페이지 분할, 생성 후 Office 열기 검사
- 백업·복원·종료·실패 작업 상태·재시도

## 확인한 테스트

- 자동 테스트 15개 통과: 십진수, 단가 누락, 월 경계, 수정 마감, 중복 파일, 원자적 반영, 오래된 미리보기 차단, 수정 이력, 백업 복원, 양식 자동 반영 안전장치 등
- React 타입 검사 및 프로덕션 빌드 성공
- 가상 1개 품목: Excel 계산 결과와 확정 금액 대사, PPT 본문 5장 및 내장 2장 자료 10개 Office 열기 성공
- 가상 70개 품목: 본문 7장 자동 확장, 미정산 표 페이지 분할 및 렌더링 확인
- 단일 실행 파일: 격리 저장소에서 화면/API·마감 계산·보고서 생성 및 종료 후 재시작 테스트
- 현재 작업 폴더에는 실제 원본 4개만 등록. 가상 테스트 거래는 운영 DB에 넣지 않음

## 아직 완료되지 않은 사항

1. **실제 8월 원본 대사와 기준 확정**: 촉매 관리 현황·단가표·입고 실적·주문전송·전월 적송재고·이전 마감 원본 필요. 현재 엑셀과 PPT의 작성 시점 차이를 기준으로 수치를 자동 정답 처리하지 않음.
2. **출하 배부 및 발주납품 전용 변환**: 제공된 복잡한 다중 표의 업무 의미·키·배부 규칙 확인 필요. 일반 행 기반 입력과 수동 정규화 입력만 완성.
3. **내장 계산서의 다페이지 분할**: 현재 거래처별 기존 2장 구조 유지. 상세 행이 많으면 계산서 이미지가 축소됨. 본문 미정산 표의 추가 페이지 기능과는 별개.
4. **장문 비고와 모든 서식 변형**: 모든 문구 길이·Office 버전·신규 거래처에 대한 출력 검증은 미완료. 본문 표가 페이지를 넘는 경우 배포 전에 실패로 처리.
5. **무인 운영 전체 검증**: 실사용 월별 자료와 다양한 수정본을 투입한 사용자 인수 테스트 필요. 과거 누적 시트의 수동 수치·피벗 및 출하 배부 의미는 별도 확인 필요.

따라서 현재 상태는 **실행 가능한 1차 구현**이며, 계획 전체의 운영 검증 완료를 의미하지 않습니다.

## 2026-09-21 BOM 연결·자료 취소 추가

- BOM 마스터 / ERP 구매단가 어댑터, 명시적 적용 기준일, 단가 변경 미리보기, 원본별 이력 및 백업 추가.
- 등록 취소·복원 UI/API 추가. 연결 자료 묶음·파생 직접 수정의 영향 표시, 재수집 중복 방지, 교체된 과거 배치 부활 방지, 확정 마감 보존.
- 실제 BOM 195품번·153마감처 연결과 구매단가 195개를 검증. 구매단가 원본은 `촉매 월계획` 프로젝트 폴더에서 발견하여 읽기 전용으로 가져옴. 운영 DB에는 원본 2개만 등록했으며 단가 적용은 사용자 기준일 확인 대기.
- 자동 테스트 46개 통과. 실제 DB 복사본의 반영·취소·복원에서 거래처 합계, 확정 이력, 예상 단가 유지 확인. 원본 엑셀 및 매크로 수정·실행 없음.
- 프론트엔드 타입 검사·빌드 및 windowed exe 패키징 성공. 새 `촉매마감관리_BOM연결.exe` 실행은 PC 애플리케이션 제어 정책의 WinError 4551로 차단됨. 보안 설정 우회하지 않음. 실행 파일 구동 및 화면 실사용 검증은 미완료.
